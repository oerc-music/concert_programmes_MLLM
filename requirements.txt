# Core web app
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# MLLM provider (Google Gemini free tier)
# generate_content / response_schema call shape verified against this range
google-genai>=1.20.0,<2.0

# Image handling
pillow>=10.2,<11.0
opencv-python-headless>=4.10,<5.0

# Local convenience: read .env for API keys without exporting shell vars
python-dotenv>=1.0,<2.0
